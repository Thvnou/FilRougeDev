/* ========================================================================
   config.js
   Configuration globale du front YMMO.
   Centralise l'URL de l'API afin de pouvoir la modifier en un seul endroit
   (par exemple si le backend n'est plus servi sur localhost:8000).
   ======================================================================== */

const API_BASE_URL = "http://localhost:8000";
